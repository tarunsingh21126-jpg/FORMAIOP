import {api} from './api';export const getForms=()=>api.get('/forms').then(r=>r.data.data);export const getForm=id=>api.get(`/forms/${id}`).then(r=>r.data.data);export const createForm=d=>api.post('/forms',d).then(r=>r.data.data);export const updateForm=(id,d)=>api.put(`/forms/${id}`,d).then(r=>r.data.data);export const duplicateForm=id=>api.post(`/forms/${id}/duplicate`).then(r=>r.data.data);export const deleteForm=id=>api.delete(`/forms/${id}`).then(r=>r.data);
