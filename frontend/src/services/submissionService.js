import {api} from './api';export const listSubmissions=params=>api.get('/submissions',{params}).then(r=>r.data.data);export const getSubmission=id=>api.get(`/submissions/${id}`).then(r=>r.data.data);export const createSubmission=d=>api.post('/submissions',d).then(r=>r.data.data);export const updateSubmission=(id,d)=>api.put(`/submissions/${id}`,d).then(r=>r.data.data);export const deleteSubmission=id=>api.delete(`/submissions/${id}`).then(r=>r.data);export const submitSubmission=id=>api.post(`/submissions/${id}/submit`).then(r=>r.data.data);
