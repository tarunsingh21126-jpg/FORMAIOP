import {NavLink,useNavigate} from 'react-router-dom';import {useAuthStore} from '../store/authStore';
const nav=[['dashboard','Dashboard','⌂'],['forms','Forms','▦'],['forms/new','New Form','＋'],['drafts','Drafts','◷'],['submissions','Submissions','✓'],['templates','Templates','▤']];
export default function AppShell({children}){const {user,logout}=useAuthStore();const navg=useNavigate();const doLogout=async()=>{await logout();navg('/signin')};return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">F</span><span>FORMA <b>AI</b></span></div><nav>{nav.map(([to,label,icon])=><NavLink key={to} to={'/'+to} className={({isActive})=>isActive?'active':''}><span>{icon}</span>{label}</NavLink>)}</nav><div className="sidebar-bottom"><NavLink to="/profile">Profile</NavLink><NavLink to="/settings">Settings</NavLink><button onClick={doLogout}>↪ Logout</button></div></aside><div className="main-shell"><header className="topbar"><div className="mobile-brand">FORMA <b>AI</b></div><div className="top-search">⌕ <span>Search forms, submissions...</span></div><div className="top-user"><div className="avatar">{user?.name?.[0]?.toUpperCase()||'U'}</div><span>{user?.name||'User'}</span></div></header><main>{children}</main></div></div>}
