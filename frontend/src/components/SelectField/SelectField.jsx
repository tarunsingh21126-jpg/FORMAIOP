import {buildValidationRules} from '../TextField/TextField';export default function SelectField({field,register,error,aiField}){return <div className="form-field"><label htmlFor={field.name}>{field.label}{field.required&&<span className="required-mark"> *</span>}{aiField&&<span className="ai-badge">✦ AI Extracted</span>}</label><select id={field.name} {...register(field.name,buildValidationRules(field))} className={error?'input-error':''}><option value="">Select an option</option>{(field.options||[]).map(o=><option key={o} value={o}>{o.replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase())}</option>)}</select>{error&&<span className="field-error">{error.message}</span>}</div>}
