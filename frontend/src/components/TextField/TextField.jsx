export default function TextField({field,register,error,type='text',aiField}){return <div className="form-field"><label htmlFor={field.name}>{field.label}{field.required&&<span className="required-mark"> *</span>}{aiField&&<span className="ai-badge">✦ AI Extracted</span>}</label><input id={field.name} type={type} placeholder={field.placeholder} {...register(field.name,buildValidationRules(field))} className={error?'input-error':''}/>{field.helpText&&<small>{field.helpText}</small>}{error&&<span className="field-error">{error.message}</span>}</div>}
export function buildValidationRules(field){const v=field.validation||{};const r={};if(field.required||v.required)r.required='This field is required';if(v.minLength!==undefined)r.minLength={value:v.minLength,message:`Minimum length is ${v.minLength}`};if(v.maxLength!==undefined)r.maxLength={value:v.maxLength,message:`Maximum length is ${v.maxLength}`};if(v.min!==undefined)r.min={value:v.min,message:`Minimum value is ${v.min}`};if(v.max!==undefined)r.max={value:v.max,message:`Maximum value is ${v.max}`};if(v.pattern)r.pattern={value:new RegExp(v.pattern),message:'Invalid format'};return r;}
