import {buildValidationRules} from '../TextField/TextField';export default function RadioField({field,register,error,aiField}){return <div className="form-field"><span className="field-label-static">{field.label}{field.required&&<span className="required-mark"> *</span>}{aiField&&<span className="ai-badge">✦ AI Extracted</span>}</span><div className="radio-group">{(field.options||[]).map(o=><label key={o} className="choice-card"><input type="radio" value={o}{...register(field.name,buildValidationRules(field))}/><span>{o.replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase())}</span></label>)}</div>{error&&<span className="field-error">{error.message}</span>}</div>}
