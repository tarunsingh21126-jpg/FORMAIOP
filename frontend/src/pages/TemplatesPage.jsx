import {Link} from 'react-router-dom';import AppShell from '../layout/AppShell';import {Card} from '../components/UI';export default function TemplatesPage(){const templates=[['Insurance Claim','Adaptive vehicle incident and damage claim.','insurance-claim','18 fields'],['Healthcare Intake','A future-ready schema for patient intake workflows.','healthcare-intake','Template'],['Customer Onboarding','A flexible onboarding workflow for customer data.','customer-onboarding','Template'],['Incident Report','Capture operational incidents with adaptive branching.','incident-report','Template']];return <AppShell><div className="page-heading"><div><span className="eyebrow">START FASTER</span><h1>Templates</h1><p>Use a schema as a starting point, then make it your own.</p></div></div><div className="template-grid">{templates.map(([t,d,id,m],i)=><Card key={id}><div className="template-icon">{i===0?'✦':'▤'}</div><span className="eyebrow">{m}</span><h2>{t}</h2><p>{d}</p>{i===0?<Link className="btn btn-primary" to={`/forms/${id}/fill`}>Use Template →</Link>:<span className="muted">Schema-ready template</span>}</Card>)}</div></AppShell>}
