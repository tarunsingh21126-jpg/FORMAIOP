import {Link} from 'react-router-dom';import {useEffect,useState} from 'react';import AppShell from '../layout/AppShell';import {Card,Badge,Button,EmptyState,Loader} from '../components/UI';import {useAuthStore} from '../store/authStore';import {useFormStore} from '../store/formStore';import {listSubmissions} from '../services/submissionService';
export default function Dashboard(){const user=useAuthStore(s=>s.user);const {forms,fetchForms,loading}=useFormStore();const [subs,setSubs]=useState([]);useEffect(()=>{fetchForms();listSubmissions().then(setSubs).catch(()=>{})},[]);const drafts=subs.filter(s=>s.status==='draft');return <AppShell><div className="page-heading"><div><span className="eyebrow">WORKSPACE</span><h1>Good morning, {user?.name?.split(' ')[0]||'there'}</h1><p>Manage your forms and submissions from one place.</p></div><Link to="/forms/new" className="btn btn-primary">＋ Create Form</Link></div><div className="stats-grid"><Stat n={forms.length} label="Total Forms"/><Stat n={drafts.length} label="Active Drafts"/><Stat n={subs.filter(s=>s.status==='submitted').length} label="Submitted Forms"/><Stat n={subs.filter(s=>s.aiAssistedFields?.length).length} label="AI-Assisted Submissions"/></div><div className="quick-grid"><Link to="/forms/new" className="quick-card"><b>＋</b><span>Create a form</span><small>Build a schema-driven workflow</small></Link><Link to="/forms/insurance-claim/fill" className="quick-card"><b>✦</b><span>Start Insurance Claim</span><small>Try AI-powered form filling</small></Link><Link to="/drafts" className="quick-card"><b>◷</b><span>Continue Draft</span><small>{drafts.length?`${drafts.length} draft${drafts.length>1?'s':''} waiting`: 'No drafts yet'}</small></Link><Link to="/templates" className="quick-card"><b>▤</b><span>Use a Template</span><small>Start with a proven schema</small></Link></div><div className="two-col"><Card><div className="card-head"><div><span className="eyebrow">FORMS</span><h2>Recent Forms</h2></div><Link to="/forms">View all →</Link></div>{loading?<Loader/>:forms.length?forms.slice(0,5).map(f=><Link className="list-row" key={f.formId} to={`/forms/${f.formId}/fill`}><div className="list-icon">▦</div><div><b>{f.title}</b><span>{f.fields?.length||0} fields · v{f.version}</span></div><Badge tone={f.status==='published'?'success':'neutral'}>{f.status}</Badge><span>→</span></Link>):<EmptyState title="No forms yet" description="Create your first schema-driven workflow."/>}</Card><Card><div className="card-head"><div><span className="eyebrow">SUBMISSIONS</span><h2>Recent Activity</h2></div><Link to="/submissions">View all →</Link></div>{subs.length?subs.slice(0,5).map(s=><Link className="list-row" key={s._id} to={`/submissions/${s._id}`}><div className="list-icon">✓</div><div><b>{s.formId}</b><span>{new Date(s.updatedAt).toLocaleString()}</span></div><Badge tone={s.status==='submitted'?'success':'warning'}>{s.status.replace('_',' ')}</Badge></Link>):<EmptyState title="No submissions yet" description="Your saved drafts and submissions will appear here."/>}</Card></div></AppShell>}
function Stat({n,label}){return <Card className="stat"><span>{label}</span><strong>{n}</strong><small>Live workspace data</small></Card>}
