import {useState} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import {login,register,forgotPassword,resetPassword} from '../services/authService';import {useAuthStore} from '../store/authStore';import {messageFromError} from '../services/api';import {Button,Card} from '../components/UI';
function AuthLayout({children,title,subtitle}){return <div className="auth-page"><div className="auth-brand"><Link to="/" className="brand">FORMA <b>AI</b></Link><div className="auth-pitch"><span className="eyebrow">AI-AUGMENTED FORMS</span><h1>Complex forms, made conversational.</h1><p>Turn natural language into structured, reviewable information with a dynamic form engine built for real workflows.</p><div className="pitch-points"><span>✦ Schema-controlled AI</span><span>✓ Human review before submission</span><span>↗ Save and resume anywhere</span></div></div></div><div className="auth-main"><Card className="auth-card"><span className="eyebrow">WELCOME TO FORMA AI</span><h2>{title}</h2><p>{subtitle}</p>{children}</Card></div></div>}
export function SignIn(){const nav=useNavigate();const setAuth=useAuthStore(s=>s.setAuth);const [f,setF]=useState({email:'',password:'',remember:true});const [err,setErr]=useState('');const [loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setLoading(true);setErr('');try{setAuth(await login(f));nav('/dashboard')}catch(x){setErr(messageFromError(x))}finally{setLoading(false)}}return <AuthLayout title="Welcome back" subtitle="Sign in to manage your intelligent forms."><form onSubmit={submit} className="auth-form"><Field label="Email" type="email" value={f.email} onChange={v=>setF({...f,email:v})}/><Field label="Password" type="password" value={f.password} onChange={v=>setF({...f,password:v})}/><div className="inline-row"><label className="check"><input type="checkbox" checked={f.remember} onChange={e=>setF({...f,remember:e.target.checked})}/> Remember me</label><Link to="/forgot-password">Forgot password?</Link></div>{err&&<div className="status error">{err}</div>}<Button disabled={loading}>{loading?'Signing in...':'Sign In'}</Button><div className="auth-switch">New to Forma AI? <Link to="/signup">Create an account</Link></div></form></AuthLayout>}
export function SignUp(){const nav=useNavigate();const setAuth=useAuthStore(s=>s.setAuth);const [f,setF]=useState({name:'',email:'',password:'',confirmPassword:'',terms:false});const [err,setErr]=useState('');const [loading,setLoading]=useState(false);async function submit(e){e.preventDefault();if(!f.terms)return setErr('Please accept the terms to continue.');setLoading(true);setErr('');try{setAuth(await register(f));nav('/dashboard')}catch(x){setErr(messageFromError(x))}finally{setLoading(false)}}return <AuthLayout title="Create your account" subtitle="Start turning complex forms into simple conversations."><form onSubmit={submit} className="auth-form"><Field label="Full name" value={f.name} onChange={v=>setF({...f,name:v})}/><Field label="Email" type="email" value={f.email} onChange={v=>setF({...f,email:v})}/><Field label="Password" type="password" value={f.password} onChange={v=>setF({...f,password:v})}/><Field label="Confirm password" type="password" value={f.confirmPassword} onChange={v=>setF({...f,confirmPassword:v})}/><label className="check"><input type="checkbox" checked={f.terms} onChange={e=>setF({...f,terms:e.target.checked})}/> I agree to the terms and privacy policy.</label>{err&&<div className="status error">{err}</div>}<Button disabled={loading}>{loading?'Creating...':'Create account'}</Button><div className="auth-switch">Already have an account? <Link to="/signin">Sign in</Link></div></form></AuthLayout>}
export function ForgotPassword(){const [email,setEmail]=useState('');const [done,setDone]=useState(false);const [err,setErr]=useState('');async function submit(e){e.preventDefault();try{await forgotPassword(email);setDone(true)}catch(x){setErr(messageFromError(x))}}return <AuthLayout title="Reset your password" subtitle="We'll create reset instructions if the account exists."><form onSubmit={submit} className="auth-form"><Field label="Email" type="email" value={email} onChange={setEmail}/>{done?<div className="status success">Check your email for reset instructions. In local development, the token is printed by the backend.</div>:<Button>Send reset instructions</Button>}{err&&<div className="status error">{err}</div>}<div className="auth-switch"><Link to="/signin">← Back to sign in</Link></div></form></AuthLayout>}
export function ResetPassword(){const token=new URLSearchParams(useLocation().search).get('token')||'';const [p,setP]=useState('');const [c,setC]=useState('');const [msg,setMsg]=useState('');const [err,setErr]=useState('');const nav=useNavigate();async function submit(e){e.preventDefault();if(p!==c)return setErr('Passwords do not match');try{await resetPassword(token,p);setMsg('Password reset successfully.');setTimeout(()=>nav('/signin'),800)}catch(x){setErr(messageFromError(x))}}return <AuthLayout title="Choose a new password" subtitle="Use your secure reset token to update your password."><form onSubmit={submit} className="auth-form"><Field label="New password" type="password" value={p} onChange={setP}/><Field label="Confirm password" type="password" value={c} onChange={setC}/>{err&&<div className="status error">{err}</div>}{msg&&<div className="status success">{msg}</div>}<Button disabled={!token}>{token?'Reset password':'Missing reset token'}</Button></form></AuthLayout>}
function Field({label,type='text',value,onChange}){return <label className="input-wrap"><span>{label}</span><input required type={type} value={value} onChange={e=>onChange(e.target.value)}/></label>}
